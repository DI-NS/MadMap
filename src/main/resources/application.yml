server:
  port: 8081
spring:
  datasource:
    url:
    driver-class-name:
    username:
    password:
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  liquibase:
    enabled: false
jwt:
  secret: ""
  expiration: 3600
